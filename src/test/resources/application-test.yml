spring:
  application:
    name: tour

  datasource:
    url: jdbc:tc:mariadb:10.11.6:///tour?TC_REUSABLE=true
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        format_sql: true
    defer-datasource-initialization: true
    open-in-view: false

  ai:
    openai:
      api-key: test


management:
  endpoints:
    web:
      exposure:
        include: prometheus
  prometheus:
    metrics:
      export:
        enabled: true


jwt:
  secret: h8nQhw+w3vyuyPDdamfKAf+T88J/F5RV+JlpniTcy8U= # diff from dev & main
  expiration-time: 3600000

tour:
  api-key: test